# PostgreSQL — used by both docker-compose and the Python app
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=retail_pipeline
POSTGRES_USER=retail_user
POSTGRES_PASSWORD=changeme

# pgAdmin (optional, only for docker-compose)
PGADMIN_EMAIL=admin@retail.local
PGADMIN_PASSWORD=admin
